class VisaTimelineItem {
  final String title;
  final String dateTime;
  final String description;
  final bool isCompleted;

  VisaTimelineItem({
    required this.title,
    required this.dateTime,
    required this.description,
    required this.isCompleted,
  });
}
